name: Keep Alive

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: curl -fsS -o /dev/null -w "%{http_code}\n" https://tokenmaxxx.onrender.com/
